package com.roofingcrm.service.attachment;

/**
 * Upload entry points for attachment validation (allowed types may differ per context).
 */
public enum AttachmentUploadContext {
    LEAD_ATTACHMENT,
    JOB_ATTACHMENT,
    JOB_RECEIPT
}
